name: create vm
on: 
    push:
        branches:
            - main
run-name: create vm
jobs:
    create-vm:
        runs-on: ubuntu-latest
        environment: dev
        env:
            job-folder: iac/ec2 
        steps:
            - name: get code
              uses: actions/checkout@v4
            - name: install terraform
              uses: hashicorp/setup-terraform@v3
            - name: initialize terraform
              run: |
                cd ${{ env.job-folder }}
                terraform init
            - name: create infra
              run: terraform apply -chdir=${{ env.job-folder }} -auto-approve
            - name: destroy infra
              if: always()
              run: terraform destroy -chdir=${{ env.job-folder }} -auto-approve
